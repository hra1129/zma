; -----------------------------------------------------------------------------
;	test program
; -----------------------------------------------------------------------------

hoge	macro	a, b, c
		defb	a, b, c
		endm

;	同じ名前のマクロを宣言しようとしてエラー
hoge	macro	a, b
		defb	a, b
		endm
