; -----------------------------------------------------------------------------
;	test program
; -----------------------------------------------------------------------------

; 範囲外の値
	ld		a, 1000
	ld		hl, 99999

; 不正な命令
	ld		[ix+1],hl

	ld		a, 0A0h
	ld		a, 10h
	ld		a, 20h
	ld		a, A0h

; 未定義ラベル
	error	aaa

; 値が確定しないラベル
	xxx					; 不正な命令
l1:
	error	l1
